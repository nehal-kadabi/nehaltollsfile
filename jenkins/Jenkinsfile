pipeline{
    stages{
        stage('git-checkout'){
            steps{
                git branch: 'main', url: ''
            }
        }

        stage('compile'){
            steps{
                sh 'mvn compile'
            }
        }

        stage('Build'){
            steps{
                sh 'mvn clean package'
            }
        }

    }
}
